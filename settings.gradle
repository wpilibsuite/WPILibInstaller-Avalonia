pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            name 'artifactory'
            url 'https://frcmaven.wpi.edu/artifactory/plugins'
        }
    }
    resolutionStrategy {
        eachPlugin {
            if ( requested.id.id == 'edu.wpi.first.GradleRIO2027' ) {
                useModule( "edu.wpi.first.GradleRIO2027:edu.wpi.first.GradleRIO2027.gradle.plugin:$gradleRioVersion" )
            }
        }
    }
}

include 'gradleriobase'
include 'apps:MavenMetaDataFixer'
include 'WPILibShortcutCreator'
