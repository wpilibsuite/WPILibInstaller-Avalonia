pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            name = 'artifactory'
            url = 'https://frcmaven.wpi.edu/artifactory/plugins'
        }
    }
    resolutionStrategy {
        eachPlugin {
            if ( requested.id.id == 'org.wpilib.GradleRIO' ) {
                useModule( "org.wpilib.GradleRIO:org.wpilib.GradleRIO.gradle.plugin:$gradleRioVersion" )
            }
        }
    }
}

include 'apps:MavenMetaDataFixer'
include 'WPILibShortcutCreator'
